interface:
  display_name: "問題調査"
  short_description: "保持済み観測を優先し、必要な再現と反証から根本原因を特定する"
  default_prompt: "$investigate-problem を使って、保持済みの観測証拠を先に確認し、必要な再現と反証から根本原因を報告してください。"

policy:
  allow_implicit_invocation: true
