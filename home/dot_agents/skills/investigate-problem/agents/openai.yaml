interface:
  display_name: "問題調査"
  short_description: "症状を再現し、観測証拠と反証から技術的な根本原因を特定する"
  default_prompt: "$investigate-problem を使って、この問題を再現し、根本原因と根拠を報告してください。"

policy:
  allow_implicit_invocation: true
