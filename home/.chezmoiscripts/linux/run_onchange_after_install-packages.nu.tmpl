#!/usr/bin/env nu

const packages = [
  {{ range $index, $element := .packages.linux.ubi.essentials }}
  "{{ $element }}"
  {{ end }}
]

$packages | each {|p|
  if (which $p | is-empty) {
    ubi -p $p.project
  }
}
