### Homebrew
if [[ -f /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# Launch Nushell only in an interactive shell
if [[ $- == *i* ]]; then
  XDG_CONFIG_HOME="$HOME/.config" exec nu
fi
